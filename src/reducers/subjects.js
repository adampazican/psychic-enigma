import ADD_SUBJECTS from '../actions/action-types'

function subjectsReducer(state = [], action) {
    switch (action) {
        default:
            return state
    }
}

export default subjectsReducer